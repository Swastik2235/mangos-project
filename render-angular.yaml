services:
  - type: static
    name: mangos-admin
    env: node
    rootDir: ./mangos-angular/mangOS-admin-main
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist/mangosAdmin
    plan: free
    envVars:
      - key: NODE_ENV
        value: production