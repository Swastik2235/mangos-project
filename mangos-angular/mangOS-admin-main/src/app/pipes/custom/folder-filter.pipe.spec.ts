import { FolderFilterPipe } from './folder-filter.pipe';

describe('FolderFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FolderFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
